server:
  port: 80


spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: 129.211.28.174
      port: 8500
      discovery:
        service-name: ${spring.application.name}

